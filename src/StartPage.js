function StartPage ({ handleStart }) {
    
    return (
        <>
            <p>Hello World</p>
            <button onClick={handleStart}>Start Quiz</button>
        </>
    )
}

export default StartPage